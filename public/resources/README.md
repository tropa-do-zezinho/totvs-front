# resources

Ativos de marca servidos estaticamente (ex.: `logo.svg`, `logo-mark.svg`).

Coloque a logo da empresa aqui e referencie no código como `/resources/<arquivo>`
(ex.: `<Image src="/resources/logo.svg" ... />`).
